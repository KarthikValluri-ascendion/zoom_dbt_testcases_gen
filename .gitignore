# dbt build artifacts
target/
dbt_packages/
logs/
.user.yml
# python
__pycache__/
*.pyc
.venv/
venv/
# local env / secrets (never commit real credentials)
.env
# local convenience wrapper: loads Snowflake creds from ~/.snowflake/connections.toml
zttd_conn.py
# generated executive dashboard output is fine to ignore by default
# (CI regenerates these via the grade step and uploads them as a build artifact)
artifacts/

# OS
.DS_Store
Thumbs.db
