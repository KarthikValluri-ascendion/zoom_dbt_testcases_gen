version: 2

# AUTO-GENERATED functional test stub by zoom-ttd (scaffold).
# Review and tighten -- then `dbt build` will pass the coverage gate.
models:
  - name: fct_meetings
    description: "TODO: document fct_meetings."
    columns:
      - name: meeting_id
        data_tests: [not_null, unique]
      - name: account_id
        data_tests: [not_null]

# A characterization unit test (mocked inputs -> captured output) is generated
# separately into _ttd_unit__fct_meetings.yml by `/zoom-ttd:gen-unit-tests`.
